#include <stdio.h>
/**
 * main - print all single digit numbers of base
 * 10 starting from zero followed by a new line
 * Return: 0
 */
int main(void)
{int i;
for (i = 0; i < 10; i++)
{
printf("%i"; i);
}
printf('\n');
return (0);
}
